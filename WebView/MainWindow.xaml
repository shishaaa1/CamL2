<Window x:Class="WebView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WebView"
        mc:Ignorable="d"
        Title="Мастер Камера - Мониторинг" 
        Height="900" Width="1200"
        MinHeight="900" MinWidth="1200"
        WindowStartupLocation="CenterScreen"
        Icon="/WebView;component/Images/cam.png"
        Background="#0F0F1A"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="CameraCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderBrush" Value="#2A2A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="15" Opacity="0.3" ShadowDepth="5"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusIndicatorStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <Style x:Key="StatPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="#22223B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#161625" CornerRadius="20" Margin="10,10,10,5" Padding="25,15">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="20" Opacity="0.4" ShadowDepth="4"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="📷 Мастер Камера" FontSize="28" FontWeight="ExtraBold" VerticalAlignment="Center" Foreground="#00D9FF"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#25253D" CornerRadius="10" Padding="12,8" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse x:Name="WsIndicator" Style="{StaticResource StatusIndicatorStyle}" Fill="#FF4757"/>
                            <TextBlock x:Name="WsStatusText" Text="Отключено" VerticalAlignment="Center" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#25253D" CornerRadius="10" Padding="12,8" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Фрейм: " FontSize="14" VerticalAlignment="Center" Foreground="#A0A0B0"/>
                            <TextBlock x:Name="CurrentFrameText" Text="0" VerticalAlignment="Center" Foreground="#00FF88" FontWeight="Bold" FontSize="15" Margin="5,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#25253D" CornerRadius="10" Padding="12,8">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🕐 " FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock x:Name="CurrentTimeText" Text="--:--:--" VerticalAlignment="Center" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <ItemsControl x:Name="CamerasItemsControl" Grid.Row="1" Margin="0,10,0,5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CameraCardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center"/>

                                <Border Grid.Column="1" CornerRadius="10" Padding="12,4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#2A2A4A"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMaster}" Value="True">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                <GradientStop Color="#00D9FF" Offset="0"/>
                                                                <GradientStop Color="#00FF88" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock x:Name="BadgeText" FontWeight="Bold" FontSize="11" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="ВЕДОМАЯ"/>
                                                <Setter Property="Foreground" Value="#A0A0B0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMaster}" Value="True">
                                                        <Setter Property="Text" Value="👑 МАСТЕР"/>
                                                        <Setter Property="Foreground" Value="#0F0F1A"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </Grid>

                            <Border Grid.Row="1" Background="#121220" CornerRadius="8" Padding="10" Margin="0,5,0,10">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse>
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse" BasedOn="{StaticResource StatusIndicatorStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                        <Setter Property="Fill" Value="#00FF88"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#00FF88" BlurRadius="8" ShadowDepth="0"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsOnline}" Value="False">
                                                        <Setter Property="Fill" Value="#FF4757"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock VerticalAlignment="Center" FontWeight="Medium">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Оффлайн"/>
                                                <Setter Property="Foreground" Value="#FF4757"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                        <Setter Property="Text" Value="Онлайн"/>
                                                        <Setter Property="Foreground" Value="#00FF88"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource StatPanelStyle}">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding MessageCount}" FontSize="22" FontWeight="Bold" Foreground="#00D9FF" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Всего" FontSize="10" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource StatPanelStyle}">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ValidCount}" FontSize="22" FontWeight="Bold" Foreground="#00FF88" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Валидных" FontSize="10" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource StatPanelStyle}">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding InvalidCount}" FontSize="22" FontWeight="Bold" Foreground="#FF4757" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Брак" FontSize="10" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource StatPanelStyle}">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding SuccessPercent, StringFormat={}{0:F1}\%}" FontSize="22" FontWeight="Bold" Foreground="#FFA502" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Успех" FontSize="10" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="2" Margin="10,5,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#161625" CornerRadius="16" Padding="20" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="15" Opacity="0.3" ShadowDepth="4"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="📨 Поток сообщений" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                        <Border Background="#2A2A4A" CornerRadius="8" Padding="12,6" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock x:Name="FrameTimerText" Text="Ожидание: 100 мс" Foreground="#FFA502" FontSize="13" FontWeight="SemiBold"/>
                        </Border>
                    </Grid>

                    <Border Grid.Row="1" CornerRadius="10" ClipToBounds="True" Background="#1A1A2E" BorderBrush="#2A2A4A" BorderThickness="1">
                        <DataGrid x:Name="MessagesDataGrid"
                                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" 
                                  CanUserResizeRows="False" CanUserSortColumns="False" IsReadOnly="True"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="#1A1A2E" AlternatingRowBackground="#22223B"
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#2A2A4A" VerticalGridLinesBrush="Transparent">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#25253D"/>
                                    <Setter Property="Foreground" Value="#00D9FF"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="15,12"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#33334C"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                    <Setter Property="Padding" Value="15,10"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#00D9FF"/>
                                            <Setter Property="Foreground" Value="#0F0F1A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Время" Binding="{Binding Time}" Width="100"/>
                                <DataGridTextColumn Header="ID" Binding="{Binding CameraId}" Width="60"/>
                                <DataGridTextColumn Header="Данные" Binding="{Binding Data}" Width="*"/>
                                <DataGridTemplateColumn Header="Статус" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="6" Padding="10,4" HorizontalAlignment="Left">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsValid}" Value="True">
                                                                <Setter Property="Background" Value="#1A00FF88"/>
                                                                <Setter Property="BorderBrush" Value="#00FF88"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsValid}" Value="False">
                                                                <Setter Property="Background" Value="#1AFF4757"/>
                                                                <Setter Property="BorderBrush" Value="#FF4757"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock FontWeight="SemiBold" FontSize="11">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsValid}" Value="True">
                                                                    <Setter Property="Text" Value="✓ ВАЛИДНО"/>
                                                                    <Setter Property="Foreground" Value="#00FF88"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsValid}" Value="False">
                                                                    <Setter Property="Text" Value="✗ БРАК"/>
                                                                    <Setter Property="Foreground" Value="#FF4757"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Результат" Binding="{Binding Result}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#161625" CornerRadius="16" Padding="20" Margin="0,0,0,10">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="15" Opacity="0.3" ShadowDepth="4"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="📊 Сводка" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                        <UniformGrid Columns="2" Rows="2">
                            <Border Style="{StaticResource StatPanelStyle}">
                                <StackPanel>
                                    <TextBlock x:Name="TotalMessagesText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#00D9FF" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Всего сообщений" FontSize="11" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource StatPanelStyle}">
                                <StackPanel>
                                    <TextBlock x:Name="ValidMessagesText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#00FF88" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Валидных" FontSize="11" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource StatPanelStyle}">
                                <StackPanel>
                                    <TextBlock x:Name="InvalidMessagesText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#FF4757" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Брак" FontSize="11" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource StatPanelStyle}">
                                <StackPanel>
                                    <TextBlock x:Name="MessagesPerSecText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#FFA502" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Сообщ / сек" FontSize="11" Foreground="#A0A0B0" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="#161625" CornerRadius="16" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="15" Opacity="0.3" ShadowDepth="4"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="📋 Журнал событий" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                        <ListBox Grid.Row="1" x:Name="LogListBox" Background="Transparent" BorderThickness="0" 
                                 ScrollViewer.VerticalScrollBarVisibility="Auto" 
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#22223B" CornerRadius="6" Padding="12,10" Margin="0,0,5,8">
                                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="13">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding Color}"/>
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>